route:
  receiver: default
  group_by: ['alertname']
receivers:
  - name: default
    # ALERTS REACH NO ONE until you configure a receiver below. To activate,
    # uncomment ONE block below and set its URL — `route.receiver` already points here.
    #
    # webhook_configs:        # generic — works for a custom handler, or Slack/Discord
    #   - url: 'https://hooks.slack.com/services/XXX/YYY/ZZZ'
    #     send_resolved: true
    #
    # For native Slack formatting instead of a raw webhook:
    # slack_configs:
    #   - api_url: 'https://hooks.slack.com/services/XXX/YYY/ZZZ'
    #     channel: '#alerts'
    #     send_resolved: true
