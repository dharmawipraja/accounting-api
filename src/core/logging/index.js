/**
 * Logging System Exports
 *
 * Central export point for logging utilities.
 */

import logger, { createLogger } from './logger.js';

// Main logger instance
export default logger;
export { createLogger, logger };
